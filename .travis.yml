language: python
python:
    - "3.3"
    - "3.4"
install: pip install -r requirements-test.txt --use-mirrors
script: ./run-tests.sh
after_success:
    - coveralls
notifications:
    email:
        - twneale@gmail.com
